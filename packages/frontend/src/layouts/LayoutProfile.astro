<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Perfil</title>
    <link rel="icon" href="/favicon-32x32.png" />
  </head>

  <body class="min-h-screen bg-muted/40">
    <!-- Fondo tipo dashboard (suave, cálido) -->
    <div class="min-h-screen bg-[radial-gradient(ellipse_at_top,_rgba(253,230,220,0.55),_transparent_60%),radial-gradient(ellipse_at_bottom,_rgba(253,230,220,0.45),_transparent_55%)]">
      <main class="mx-auto w-full max-w-6xl px-4 py-8 md:px-8">
        <!-- Header -->
        <header class="mb-6">
          <h1 class="text-3xl font-semibold text-slate-800">Configuración</h1>
          <p class="text-sm text-slate-500">
            Gestioná tu cuenta y la información de tu empresa
          </p>
        </header>

        <!-- Layout principal: sidebar + contenido -->
        <div class="grid gap-6 md:grid-cols-[260px_1fr]">
          <!-- Sidebar -->
          <aside class="h-fit rounded-2xl border bg-white p-4 shadow-sm">
            <div class="mb-4">
              <div class="text-base font-semibold text-slate-800">Menu</div>
              <div class="text-xs text-slate-500">Perfil y preferencias</div>
            </div>

            <nav class="grid gap-1 text-sm text-slate-600">
              <a
                href="/profile/user"
                class="rounded-xl px-3 py-2 font-medium text-slate-900 hover:bg-slate-50"
              >
                Perfil personal
              </a>
              <a
                href="/profile/bussines"
                class="rounded-xl px-3 py-2 font-medium text-slate-900 hover:bg-slate-50"
              >
                Preferencia de empresa
              </a>
          </aside>

          <!-- Contenido -->
          <section>
            <slot name="content" />
          </section>
        </div>
      </main>
    </div>
  </body>
</html>
